# Go Script Upgrade Checklist

- [x] Add request timeout using `http.Client` instead of direct `http.Get`
- [x] Validate HTTP status codes before parsing response body
- [x] Remove raw API body logging or guard it behind a debug flag
- [x] Replace `map[string]any` JSON parsing with typed response structs
- [x] Handle CSV reader errors properly (differentiate EOF vs real errors)
- [x] Validate CSV row length before indexing fields
- [x] Handle `strconv.Atoi` conversion errors for quantity
- [x] Check all `csv.Writer.Write` errors
- [x] Check `writer.Error()` after `Flush`
- [x] Include `Cmc` in output CSV header and row if needed
- [x] Stop ignoring errors from read and write calls in `main`
- [x] Return slices from functions instead of passing pointer-to-slice
- [x] Rename fields/functions to Go style (camelCase, no underscores)
- [x] Add CLI flags for input file, output file, API base URL, timeout
- [x] Add concurrency with a bounded worker pool for API calls
- [x] Add caching by Scryfall ID to avoid duplicate API calls
- [x] Add unit tests for parsing and conversion logic
- [x] Add `httptest`-based API tests (200, 500, timeout, invalid JSON)
- [x] Add integration test for CSV input-to-output flow
- [x] Add structured logging with row/card context for failures
